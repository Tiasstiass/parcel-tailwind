{%- comment -%}
  Snippet for keep critical css
  Inputs:
    nothing
{%- endcomment -%}

<style id="critical-css" type="text/css">
/* ---------------------- HTML & BODY ---------------------- */
  *, *:before, *:after {
    box-sizing: border-box;
  }
  *:focus,
  *:active {
    outline: none;
    border: none;
  }
  html { box-sizing: border-box; }
  body {
    margin: 0px;
    padding: 0px;
    background: var(--body-bg);
    color: var(--text-color-primary);
    font-family: var(--font-main);
    font-size: var(--font-main-size);
    position: relative;
  }
    /* -- Paragraphs -- */
  p { font-family: var(--font-text); }
    /* -- Links -- */
  a {
    color: var(--text-color-primary);
    text-decoration: none;
  }
  /* -- Lists -- */
  ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  /* -- Input - Chrome, Safari, Edge, Opera -- */
  input::-webkit-outer-spin-button,
  input::-webkit-inner-spin-button {
    -webkit-appearance: none;
    margin: 0;
  }
  /* -- Input - Firefox-- */
  input[type=number] {
    -moz-appearance: textfield;
  }
/* --------------------------------------------------------- */

/* ---------------------- TYPOGRAPHY ----------------------- */

/* --Default H1 -> H5 -&- General Heading Classes -- */
  h1, h2, h3, h4, h5,
  .heading-h1, .heading-h2, .heading-h3, .heading-h4, .heading-h5 {
    color: var(--heading-color);
    font-family: var(--font-main);
    margin-top: 0px;
  }

  /* --Helper Text Elements -- */
  .image-caption, .button-text, .title-tag { font-weight: 400; }
  .text-medium, .text-small { font-weight: 500; }
  .button-text, .text-medium, .button-primary, .button-secondary { font-size: clamp(13px, 1.2vw, 15px); line-height: 26px; }
  .text-small { font-size: 13px; line-height: 22px; }
  .title-tag  { font-size: 10px; line-height: 17px; letter-spacing: 2.43px; font-family: var(--font-main); text-transform: uppercase; }
  .text-body, .text-body p {
    font-family : var(--font-text) !important;
    font-weight: 500;
    font-size: 18px;
    line-height: 31px;
    margin: 0;
   }

  .image-caption {
    font-family: var(--text-family-regular);
    font-style: italic;
    font-size: 13px;
    line-height: 22px;
  }

  /* --Media Rules -- */
  @media (max-width: 767px) {
    .image-caption {
      font-size: 11px;
      line-height: 19px;
    }
    .button-text, .button-primary, .button-secondary {
      font-size: 13px;
      line-height: 22px;
    }
    .text-medium {
      font-size: 13px;
      line-height: 22px;
    }
    .text-small {
      font-size: 11px;
      line-height: 19px;
    }
    .title-tag {
      font-size: 9px;
      line-height: 15px;
    }
  }

  .link-bold {
    text-decoration: underline;
    font-style: italic;
    font-family: var(--font-main);
    font-size: 12px;
  }
/* --------------------------------------------------------- */

/* ----------------------- ELEMENTS ------------------------ */

/* -- Borders -- */
  .border-primary   { border: 1px solid var(--color__deep-teal); }
  .border-secondary { border: 1px solid var(--color__sea-mist) ; }

/* --Buttons -- */
  button, .btn, .button-primary, .button-secondary, .button {
    font-family: var(--font-main);
    font-size: 0.9375rem;
    padding: 0.75rem 2.125rem;
    /* margin-right: clamp(0.4rem, 1vw, 1rem); */
    border: none;
    border-radius: 50px;
    cursor: pointer;
    -webkit-transition: background-color 200ms ease, color 200ms ease;
    -o-transition: background-color 200ms ease, color 200ms ease;
    transition: background-color 200ms ease, color 200ms ease;
    width: fit-content;
    text-align: center;
    display: inline-block;
  }
  .button-primary, .button.cta {
    background-color: var(--color__button-primary-background);
    color: var(--color__button-primary-font);
  }
  .button-secondary, .button {
    background-color: var(--color__button-secondary-background);
    color: var(--color__button-secondary-font);
  }
  button:hover, .button-primary:hover, .button-secondary:hover, .button.cta:hover {
    background-color: var(--color__button-hover);
    color: var(--color--light);
  }

/* --------------------------------------------------------- */

  .page-width, .shopify-app-block {
    max-width: var(--content-width-desktop);
    margin-left: auto;
    margin-right: auto;
  }

  .price {
    color: var(--price-color);
    font-family: var(--font-main);
  }

  .section-container {
    padding: 20px 0 20px 0;
  }

  img, svg, video, canvas, audio, iframe, embed, object {
    max-width: 100%;
    max-height: 100%;
    object-fit: cover;
    border: none;
    vertical-align: middle;
  }

  input, optgroup, select, textarea {
    padding: 17px 20px !important;
    border: 1px solid var(--color__general-border) !important;
    font-size: 12px !important;
    font-family: var(--font-main);
    color: var(--text-color-primary) !important;
    min-width: 220px;
    cursor: pointer;
    border-radius: 3px;
  }

  button.icon {
    min-width: auto;
    padding: initial !important;
  }

  select {
    -webkit-appearance: none;
    -moz-appearance: none;
    appearance: none;
    color: var(--font-text) !important;
    background: none !important;
    border: none !important;
    font-family: var(--font-text);
    transition: all 0.1s;
    font-size: 15px !important;
    font-weight: bold;
    padding-right: 35px !important;
  }

  .product__variant select {
    height: 100%;
    min-width: initial;
    width: 100%;
  }

  .select-wrapper select {
    text-align: center;
    padding-right: 32px !important;
    position: relative;
    z-index: 2;
  }

  .select-wrapper {
    position: relative;
  }

  .select-wrapper .arrow {
    position: absolute;
    right: 8px;
    top: 50%;
    transform: translateY(-43%) !important;
    z-index: 1;
  }

  select::-ms-expand {
     display: none;
  }

  ::placeholder {
    font-family: var(--font-heading);
  }

  .grid > .button, .flex > .button {
    min-width: initial;
  }

  @media (max-width: 767px) {
    input, optgroup, select, textarea {
      min-width: 180px;
    }
  }

  @media (max-width: 380px) {
    .button, .btn, button, input, optgroup, select, textarea {
      min-width: 140px;
    }
  }

  .button.small {
    padding: 5px 20px 5px 20px !important;
    font-size: 12px !important;
  }
  @media (max-width: 767px) {
    .button.small {
      padding: 5px 20px 5px 20px !important;
      font-size: 12px !important;
    }
  }

  .font-text {
    font-family: var(--font-text);
  }

  #shopify-section-layout-header + .shopify-section {
    margin-top: 0px;
  }

  @media (max-width: 767px) {
    .heading {
      font-size: var(--font-heading-size-mobile);
    }
  }

  .loading .skeleton-loading, .ajax-loading {
    position: relative;
    background-color: var(--color-loading) !important;
    min-height: 20px;
    overflow-x: hidden;
  }

  .loading .skeleton-loading.skeleton-item-1 { width: 100%; }
  .loading .skeleton-loading.skeleton-item-2 { width: 80%; }
  .loading .skeleton-loading.skeleton-item-3 { width: 50%; }

  .loading .skeleton-loading::after,
  .ajax-loading::after {
    position: absolute;
    top: 0;
    right: 0;
    bottom: 0;
    left: 0;
    transform: translateX(-100%);
    background-image: linear-gradient(
      90deg,
      rgb(255, 255, 255, 0) 0,
      rgba(109, 0, 195, 0.1) 15%,
      rgba(109, 0, 195, 0.2) 50%,
      rgba(255, 255, 255, 0)
    );
    animation: loading 3s infinite;
    content: '';
  }

  .loading .skeleton-loading button,
  .loading .skeleton-loading .button,
  .loading .skeleton-loading img {
    display: none;
  }

  @keyframes loading {
    100% {
      transform: translateX(100%);
    }
  }

  /* Margins */
  .m-1     { margin: 0.25rem;                       }
  .m-10    { margin: 2.5rem;                        }
  .mt-1    { margin-top: 0.25rem;                   }
  .mt-2    { margin-top: 0.5rem;                    }
  .mb-3    { margin-bottom: 0.75rem;                }
  .mt-3    { margin-top: 0.75rem;                   }
  .mb-5    { margin-bottom: 1.25rem;                }
  .mb-10   { margin-bottom: 2.5rem;                 }
  .mx-auto { margin-left: auto; margin-right: auto; }

  /* Layout & Alignment */
  .justify-end    { justify-content: flex-end;     }
  .justify-center { justify-content: center;       }
  .justify-stretch { justify-content: stretch;       }
  .justify-around { justify-content: space-around; }
  .items-center   { align-items: center;           }
  .text-center    { text-align: center;            }

  .block  { display: block; }
  .flex   { display: flex; }
  .grid   { display: grid; }
  .hidden { display: none !important; }
  .justify-between { justify-content: space-between; }
  .flex-wrap { flex-wrap: wrap; }
  .relative { position: relative; }
  .h-20 { height: 5rem; }
  .p-3 { padding: 0.75rem; }
  .p-4 { padding: 1rem; }
  .p-5 { padding: 1.25rem; }
  .p-10 { padding: 2.5rem; }
  .pt-5 { padding-top: 1.25rem; }
  .pt-10 { padding-top: 2.5rem; }
  .pb-10 { padding-bottom: 2.5rem; }
  .px-4 { padding-left: 1rem; padding-right: 1rem; }
  .px-5 { padding-left: 1.25rem; padding-right: 1.25rem; }
  .px-5 { padding-left: 1.50rem; padding-right: 1.50rem; }
  .px-6 { padding-left: 1.75rem; padding-right: 1.75rem; }
  .py-3 { padding-top: 0.75rem; padding-bottom: 0.75rem; }
  .py-5 { padding-top: 1.25rem; padding-bottom: 1.25rem; }
  .gap-2 { grid-gap: 0.5rem; gap: 0.5rem; }
  .gap-5 { grid-gap: 1.25rem; gap: 1.25rem; }
  .gap-7 { grid-gap: 1.75rem; gap: 1.75rem; }
  .gap-8 { grid-gap: 2rem; gap: 2rem; }
  .gap-10 {  grid-gap: 2.5rem; gap: 2.5rem; }
  .grid-cols-2 { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .grid-cols-3 { grid-template-columns: repeat(3, minmax(0, 1fr)); }
  .grid-cols-4 { grid-template-columns: repeat(4, minmax(0, 1fr)); }

  @media (min-width: 1280px) {
    .xl\:justify-center {
      justify-content: center;
    }
  }

  @media (min-width: 1024px) {
    .lg\:hidden { display: none !important;  }
    .lg\:block  { display: block !important; }
  }

  @media (min-width: 768px) {
    .md\:block       { display: block !important;       }
    .md\:flex        { display: flex !important;        }
    .md\:hidden      { display: none !important;        }
    .md\:inline-flex { display: inline-flex !important; }

    .md\:justify-between { justify-content: space-between !important; }

    .md\:grid-cols-2 { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .md\:grid-cols-3 { grid-template-columns: repeat(3, minmax(0, 1fr)); }
    .md\:grid-cols-4 { grid-template-columns: repeat(4, minmax(0, 1fr)); }

    .md\:px-10 { padding-left: 2.5rem !important; padding-right: 2.5rem !important; }
    .md\:pt-5  { padding-top: 1.25rem !important;                                   }
    .md\:pt-10 { padding-top: 2.5rem !important;                                    }
    .md\:pb-5  { padding-bottom: 1.25rem;                                           }
    .md\:py-5  { padding-top: 1.25rem; padding-bottom: 1.25rem;                     }

    .md\:mb-5  { margin-bottom: 1.25rem;        }
    .md\:mt-10 { margin-top: 2.5rem !important; }

    .md\:h-32 { height: 8rem !important; }

    .md\:order-2 { order: 2; }
    .md\:order-3 { order: 3; }

    .md\:w-1\/3 { width: 33.333333%; }
    .md\:w-2\/3 { width: 66.666667%; }

    .md\:gap-5    { grid-gap: 1.25rem; gap: 1.25rem;   }
    .md\:gap-y-12 { grid-row-gap: 3rem; row-gap: 3rem; }
  }

  .shopify-policy__container {
    max-width: var(--content-width-desktop) !important;
    margin-left: auto;
    margin-right: auto;
    padding: 30px 0 0 0 !important;
  }

  .page-width-half {
    max-width: var(--content-width-half);
    margin-left: auto;
    margin-right: auto;
  }

  @media (max-width: 1460px) {
    .page-width, .page-width-half, .shopify-app-block {
      padding-left: var(--content-padding-mobile);
      padding-right: var(--content-padding-mobile);
    }
    .page-width.mobile-full {
      padding: 0px !important;
    }
    .shopify-policy__container {
      padding-left: var(--content-padding-mobile) !important;
      padding-right: var(--content-padding-mobile) !important;
    }
  }

  #main-menu__desktop, #main-menu__tablet {
    font-family: var(--font-menu);
    font-size: 13px;
    font-weight: 500;
  }

  #main-menu__desktop a,
  #main-menu__tablet a {
    color: var(--color__deep-teal);
  }

  #main-menu__desktop a:hover,
  #main-menu__desktop a.active,
  #main-menu__tablet a:hover,
  #main-menu__tablet a.active {
    color: var(--color__2);
    font-weight: 700;
    box-shadow: inset 0 -2px 0 0 var(--color__purple);
  }

  #main-menu__desktop .level1,
  #main-menu__tablet .level1 {
    overflow: hidden;
  }

  #main-menu__desktop .level1:hover,
  #main-menu__tablet .level1:hover {
    overflow: visible;
  }

  #cart-bubble-counter {
    width: 16px;
    height: 16px;
    position: absolute;
    right: -5px;
    bottom: -5px;
    color: #fff;
    background: #24706F;
    font-size: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 100%;
  }

  #cart-bubble-counter span {
    display: block;
    margin-right: -2px;
    margin-top: 1px;
  }
  @media (max-width: 768px) {
    #cart-bubble-counter span {
      padding-top: 3px;
    }
  }

  /* HEADER STYLING */
  .logo-part1 {
    font-size: 18px;
  }
  .example-section {
    margin-top: 50px;
  }

  #site-header, #top-header-container {
    border-bottom: 1px solid var(--color__sea-mist);
  }

  .header-right {
    border-right: 1px solid var(--color__sea-mist);
  }

  .header-icons div {
    height: {{ settings.header_height }}px;
    width: 76px;
    border-left: 1px solid var(--color__sea-mist);
    display: flex;
    align-items: center;
    justify-content: center;
  }
  @media (max-width: 768px) {
    .header-icons {
      padding: 0px 0px 0px 15px;
    }
  }

  .flex-col { flex-direction: column; }

  .icon {
    display: block;
    width: 20px;
    height: 20px;
    background: none;
    position: relative;
    cursor: pointer;
    transform: none !important;
    text-align: center;
    font-size: 50px !important;
    line-height: 0px;
  }

  .icon:hover {
    opacity: 0.7;
  }

  .icon.close:before, .icon.close:after {
    position: absolute;
    top: 0px;
    left: 10px;
    content: ' ';
    height: 21px;
    width: 1px;
    background-color: var(--heading-color);
  }

  .icon.close:before {
    transform: rotate(45deg);
  }

  .icon.close:after {
    transform: rotate(-45deg);
  }

  .icon.arrow:before {
    content: "";
    border: solid #707070;
    border-width: 0 2px 2px 0;
    display: inline-block;
    padding: 5px;
    position: absolute;
    top: 0px;
    left: 0px;
  }

  .icon.arrow.up:before {
    transform: rotate(-135deg);
    -webkit-transform: rotate(-135deg);
  }

  .icon.arrow.down:before {
    transform: rotate(45deg);
    -webkit-transform: rotate(45deg);
  }

  .icon.arrow.left:before {
    transform: rotate(135deg);
    -webkit-transform: rotate(135deg);
  }

  .icon.arrow.right:before {
    transform: rotate(-45deg);
    -webkit-transform: rotate(-45deg);
  }

  .product-qty__arrow svg {
    pointer-events: none;
  }

  .bundle-plus.icon.plus:before {
    content: "\208A";
    transform: translateY(0%) !important;
  }
  
  /* .icon.minus:before {
    content: "\208B"
  }
  */

  .bundle-plus.icon.plus:before {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    transform: translateY(-50%);
  }

  .product__qty {
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .product-qty__wrapper {
    position: relative;
    width: 50%;
    height: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 0 17px 0 15px;
  }

  .product-qty__wrapper > .icon {
    flex: 0 0 20px;
  }

  .product-qty__input {
    flex: 1 1 auto;
    min-width: initial;
    border: none !important;
    text-align: center;
    padding: 0px !important;
    color: var(--text-color-primary) !important;
    -moz-appearance: textfield;
    font-size: 15px !important;
    font-family: var(--font-text);
    font-weight: bold;
    background: none;
  }

  .product-qty__input::-webkit-outer-spin-button,
  .product-qty__input::-webkit-inner-spin-button {
    -webkit-appearance: none;
    margin: 0;
  }

  body.loading, body.loading *, body.loading a, body.loading input, body.loading button, body.loading div {
    cursor: wait !important;
  }

  .mt-5 {
    margin-top: 1.25rem;
  }
  .mt-10 {
    margin-top: 2.5rem;
  }

  #site-header .site-header__wrapper {
    height: {{ settings.header_height }}px;
  }

  #main-menu__desktop,
  #main-menu__tablet {
    height: {{ settings.header_menu_height }}px;
  }

  .fixed-header #site-header {
    position: fixed;
    top: 0px;
    left: 0px;
    z-index: 23;
    width: 100%;
    background: #fff;
  }

  .fixed-header #shopify-section-layout-mega-menu + div {
    margin-top: calc({{ settings.header_height }}px + {{ settings.header_menu_height }}px + 2px) !important;
  }

  @media (max-width: 767px) {
    .fixed-header #shopify-section-layout-mega-menu + div {
      margin-top: 0px !important;
    }
    #site-header > .site-header__wrapper {
      height: {{ settings.header_height }}px;
    }
    .fixed-header #site-header + div {
      margin-top: {{ settings.header_height }}px !important;
    }
  }

  .pb-3 {
    padding-bottom: 0.75rem;
  }

  .mb-3 {
    margin-bottom: 0.75rem;
  }

  .gap-y-10 {
    grid-row-gap: 2.5rem;
    row-gap: 2.5rem;
  }
  .gap-4 {
    grid-gap: 1rem;
    gap: 1rem;
  }

  @media (max-width: 767px) {
    .collection-slider .loading > div {
      display: none;
    }
    .collection-slider .loading > div:first-child {
      display: block;
    }
    .collection-slider .button {
      min-width: initial !important;
    }
    .collection--item__image img {
      object-fit: cover;
    }
    .collection--item .button {
      min-width: initial !important;
    }
  }

  .modal-popup {
    display: none;
  }

  .image-responsive__container {
    display: block;
    position: relative;
    width: 100%;
    padding-top: 56%;
  }
  .image-responsive__container.image-square {
    padding-top: 100% !important;
  }
  .image-responsive__image, .image-responsive__container > img, picture.image-responsive__image img, picture.image-responsive__image source {
    position: absolute;
    top: 0px;
    left: 0px;
    width: 100%;
    height: 100%;
  }
  @media (max-width: 767px) {
    .image-responsive__container {
      padding-top: 100% !important;
    }
  }

  .ajax-grid-pagination {
    min-height: 200px !important;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #333;
  }

  .product-price__unit {
    font-size: {{ settings.price_unit_font_size }}px;
  }

  .video-section,
  .video-wrapper,
  .video-holder,
  .video-thumbnail,
  .video-autoplay,
  .video-loading,
  .video-thumbnail img {
    height: 100%;
    overflow: hidden;
  }

  .video-holder {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .video-holder iframe {
    height: 100%;
    object-fit: initial;
  }

  .splide__pagination {
    margin-top: 10px;
    display: flex;
    justify-content: center;
    width: 100%;
    gap: 0px 20px;
  }
  .splide__pagination button {
    width: 50px !important;
    min-width: initial !important;
    height: 10px !important;
    border: none !important;
    padding: 0px !important;
    background: #f3f3f3 !important;
  }
  .splide__pagination button.is-active {
    background: #7e7e7e !important;
  }

  .scrollbar {
    position: relative;
    padding-bottom: 50px;
    margin-bottom: 30px;

    overflow: hidden !important;
    overflow-anchor: none;
    -ms-overflow-style: none;
    touch-action: auto;
    -ms-touch-action: auto;
  }

  .flex-auto {
    flex: 1 1 auto;
  }

  /* Product Grid Item */
  .product-grid-item__wrapper {
    border-top: 1px solid var(--color__border-secondary);
    border-bottom: 1px solid var(--color__border-secondary);
    border-right: 1px solid var(--color__border-secondary);
    position: relative;
    max-width: 320px;
    flex: 0 0 320px;
  }
  .product-grid-item__wrapper:nth-child(n+5) {
    border-top: none;
  }
  .collection-grid--filters .product-grid-item__wrapper:nth-child(n+4) {
    border-top: none;
  }
  .scrollbar .product-grid-item__wrapper:first-child,
  .scrollbar .product-grid-item__wrapper.first,
  .bundle-products-section .product-grid-item__wrapper.first {
    border-left: 1px solid var(--color__border-secondary);
  }
  .scrollbar .product-grid-item__wrapper {
    border-top: 1px solid var(--color__border-secondary) !important;
  }

  .product-grid__wrapper {
    border-left: 1px solid var(--color__border-secondary);
    max-width: 1280px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr 1fr 1fr;
  }
  @media (max-width: 1023px) {
    .product-grid__wrapper {
      grid-template-columns: 1fr 1fr !important;
      max-width: 640px !important;
    }
    .native-collection .product-grid__wrapper {
      max-width: 720px !important;
    }
    .product-grid-item__wrapper:nth-child(n+3) {
      border-top: none;
    }
  }
  @media (max-width: 767px) {
    .product-grid__wrapper {
      grid-template-columns: 1fr !important;
      max-width: 360px !important;
    }
    .product-grid-item__wrapper:nth-child(n+2) {
      border-top: none;
    }
  }

  .collection-grid--filters .product-grid__wrapper {
    grid-template-columns: 1fr 1fr 1fr;
    max-width: 1050px;
  }
  .native-collection .product-grid-item__wrapper {
    flex: 0 0 360px;
    max-width: 360px;
  }
  .collection-grid--filters .product-grid-item__actions form > div {
    max-width: 140px;
  }

  .product-grid-item {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    height: 100%;
    background: #fff;
  }
  .product-grid-item__tag {
    position: absolute;
    top: 30px;
    text-align: center;
    padding: 0 17px;
    font-size: 13px;
    line-height: 22px;
    font-family: var(--font-text);
    z-index: 2;
  }
  .product-grid-item__tag.heart {
    top: 20px;
    padding: 0;
    width: 30px;
    height: 30px;
    right: 25px;
    cursor: pointer;
  }
  .product-grid-item__tag.heart svg {
    pointer-events: none;
    position: absolute;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
  }
  .product-grid-item__tag.heart svg:first-of-type {
    display: block;
  }
  .product-grid-item__tag.heart svg:last-of-type {
    display: none;
  }
  .product-grid-item__tag.heart.wishlist svg:first-of-type {
    display: none;
  }
  .product-grid-item__tag.heart.wishlist svg:last-of-type {
    display: block;
  }

  .product-grid-item__tag.heart.wishlist svg path {
    /* change heart color here */
    fill: var(--color__6);
  }

  .product-grid-item__tag.new {
    color: var(--text-color-primary);
    background: var(--color__1);
    font-weight: bold;
  }
  .product-grid-item__tag.sale {
    color: #fff;
    background: var(--color__2);
  }
  .product-grid-item__meta {
    padding: 0 1.5rem;
    margin-top: 0.5rem;
    display: flex;
    flex-direction: column;
    gap: 5px;
    padding-bottom: 20px;
  }
  .product-grid-item__title {
    font-family: var(--font-text);
    font-size: 18px;
    line-height: 31px;
    color: var(--text-color-primary);
  }
  .product-grid-item__actions {
    width: 100%;
  }
  .product-grid-item__actions form {
    display: flex;
    justify-content: stretch;
    overflow: hidden;
  }
  .product-grid-item__actions form > div {
    border: 1px solid var(--color__general-border) !important;
    flex: 1 1 30%;
    max-width: 120px;
  }
  .product-grid-item__actions form > div.button {
    flex: 0 0 80px;
    max-width: 80px;
    min-width: initial !important;
    padding: 0px !important;
    margin: 0px !important;
    border: none !important;
    border-radius: 0px;
    background: var(--color__button-primary-background);
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }
  .product-grid-item__actions form > div.button svg {
    transform: translate(21%, 20%);
  }

  /* Blog Grid Item */
  .blog-grid-item__wrapper {
    flex: 0 0 320px;
  }
  @media (max-width: 767px) {
    .scrollbar > .blog-grid-item__wrapper {
      flex: 0 0 205px;
    }
  }
  .blog-grid-item__category {
    margin-top: 15px;
  }
  .blog-grid-item__title {
    margin-top: 5px;
    font-weight: 500;
    font-size: 18px;
    line-height: 31px;
    font-family: var(--font-text);
  }

  /* Product form */
  .product-details__grid {
    display: flex;
    gap: 60px;
    padding-top: 50px;
    padding-bottom: 50px;
  }
  @media (max-width: 767px) {
    .product-details__grid {
      display: block;
    }
    .product-details__blocks {
      margin-top: 20px;
    }
    .product-details__button-buy {
      width: 100%;
    }
  }
  .product-details__grid .product-details__gallery {
    flex: 0 0 40%;
  }
  .product-details__grid .product-details__blocks {
    flex: 0 0 calc(60% - 60px);
    text-align: left;
  }
  .product-details__title {
    margin: 10px 0 0 0;
  }
  .product-details__price-wrapper {
    margin-top: 30px;
  }
  .product-details__variant-wrapper {
    margin-top: 20px;
    display: flex;
  }
  .product-details__variant-wrapper > div {
    border: 1px solid var(--color__general-border);
  }
  .product-details__variant-wrapper > div:first-child {
    border-right: none;
  }
  .product-details__button-buy {
    position: relative;
    overflow: hidden;
    margin: 50px 0 0 0;
    padding: 18px 40px 17px 130px !important;
    font-size: 15px;
  }
  .product-details__button-buy span.button-line {
    height: calc(100% - 20px);
    width: 1px;
    position: absolute;
    left: 90px;
    top: 10px;
    border-left: 1px solid #fff;
  }
  .product-details__button-buy svg {
    position: absolute;
    left: 20px;
    top: 50%;
    transform: translate(27%, -30%);
  }
  .product__prices {
    display: flex;
    gap: 15px;
    align-items: baseline;
    margin-top: 15px;
  }
  .product__price {
    font-size: 22px;
    line-height: 30px;
    display: flex;
  }
  .product-details__grid .product__price {
    font-size: 31px;
    line-height: 53px;
  }
  .product__price--compare {
    text-decoration-line: line-through;
    font-size: 15px;
    line-height: 26px;
  }
  .product__price span.decimals {
    font-size: 15px;
    line-height: 26px;
    font-weight: bold;
  }
  .product__price span.currency {
    align-self: flex-end;
    margin-left: 2px;
    font-size: 15px;
    line-height: 26px;
    font-weight: bold;
  }
  .product-details__grid .product__price span.decimals {
    font-size: 22px;
    line-height: 47px;
  }
  .product-details__grid .product__price span.currency {
    font-size: 22px;
    line-height: 47px;
  }
  .product__unit {
    font-size: 13px;
    line-height: 22px;
    opacity: 0.5;
  }
  #product-main-data .shopify-app-block {
    margin: 0 0 15px 0;
    padding: 0px;
  }
  .jdgm-preview-badge {
    font-family: var(--font-text);
    font-size: 13px;
    line-height: 22px;
  }
  .jdgm-preview-badge .jdgm-star.jdgm--off {
    color: var(--text-color-primary) !important;
  }
  .jdgm-preview-badge .jdgm-star {
    color: var(--color__5) !important;
  }
  .jdgm-prev-badge__text {
    margin-left: 10px !important;
  }
  .extra-content-block {
    min-width: 300px;
    margin-right: 50px;
  }
  .extra-content-block .button {
    margin: 50px 0 0 0;
  }
  .extra-content__text {
    font-size: 18px;
    line-height: 31px;
    font-family: var(--font-text);
  }
  @media (min-width: 1200px) {
    .extra-content-block {
      min-width: 560px;
    }
  }
  .collection-grid > .extra-content-block,
  .extra-content-block.prepended {
    margin-bottom: 50px;
  }
  .review-stars-snippet .jdgm-star {
    margin-right: 7px !important;
    font-size: 16px;
  }

  .collection-grid-section {
    padding: 50px 0 30px 0;
  }

  /* SPLIDE CSS */
    .splide__container{
    position:relative;
    box-sizing:border-box
  }
  .splide__list{
    margin:0!important;
    padding:0!important;
    width:-webkit-max-content;
    width:-moz-max-content;
    width:max-content;
    will-change:transform
  }
  .splide.is-active .splide__list{
    display:flex
  }
  .splide__pagination{
    display:inline-flex;
    align-items:center;
    width:95%;
    flex-wrap:wrap;
    justify-content:center;
    margin:0
  }
  .splide__pagination li{
    list-style-type:none;
    display:inline-block;
    line-height:1;
    margin:0
  }
  .splide,.splide__slide{
    position:relative;
    outline:none
  }
  .splide__slide{
    box-sizing:border-box;
    list-style-type:none!important;
    margin:0;
    flex-shrink:0
  }
  .splide__slide img{
    vertical-align:bottom
  }
  .splide__slider{
    position:relative
  }
  .splide__spinner{
    position:absolute;
    top:0;
    left:0;
    right:0;
    bottom:0;
    margin:auto;
    display:inline-block;
    width:20px;
    height:20px;
    border-radius:50%;
    border:2px solid #999;
    border-left-color:transparent;
    -webkit-animation:splide-loading 1s linear infinite;
            animation:splide-loading 1s linear infinite
  }
  .splide__track{
    position:relative;
    z-index:0;
    overflow:hidden
  }
  .splide--draggable>.splide__track>.splide__list>.splide__slide{
    -webkit-user-select:none;
    -moz-user-select:none;
    -ms-user-select:none;
        user-select:none
  }
  .splide--fade>.splide__track>.splide__list{
    display:block
  }
  .splide--fade>.splide__track>.splide__list>.splide__slide{
    position:absolute;
    top:0;
    left:0;
    z-index:0;
    opacity:0
  }
  .splide--fade>.splide__track>.splide__list>.splide__slide.is-active{
    position:relative;
    z-index:1;
    opacity:1
  }
  .splide--rtl{
    direction:rtl
  }
  .splide--ttb>.splide__track>.splide__list{
    display:block
  }
  .splide--ttb>.splide__pagination{
    width:auto
  }


  /* Drop-down */
  .drop-down {
    position: relative;
    overflow: hidden;
  }
  .drop-down .drop-down__title {
    position: relative;
    padding: 15px 0;
    border-bottom: 1px solid var(--color__1);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: space-between;
    font-size: 12px;
  }
  .drop-down .drop-down__data {
    transition: all 0.5s;
    max-height: 0px;
    display: flex;
    flex-direction: column;
    gap: 10px;
    font-size: 15px;
    font-family: var(--font-text);
    overflow: hidden;
  }
  .drop-down.active .drop-down__data {
    max-height: 1000px;
    padding: 20px 0;
  }
  .drop-down input[type=checkbox] {
    display: none;
  }
  .drop-down label {
    display: flex;
    justify-content: flex-start;
    align-items: center;
    gap: 15px;
    cursor: pointer;
  }
  .drop-down .checkbox {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 18px;
    height: 18px;
    border: 1px solid var(--color__1);
  }
  .drop-down .checkbox .tick {
    display: none;
  }
  .drop-down input:checked + label {
    font-weight: bold;
  }
  .drop-down input:checked + label .checkbox {
    background: var(--color__5);
    border: 1px solid var(--text-color-primary);
    color: var(--text-color-primary);
  }
  .drop-down input:checked + label .checkbox .tick {
    display: inline;
  }

  /* Skeleton Animation */
  .anim-skeleton:not(.loaded) {
    background-image: linear-gradient(
      90deg,
      rgba(#fff, 0) 0,
      rgba(#fff, 0.2) 20%,
      rgba(#fff, 0.5) 60%,
      rgba(#fff, 0)
    );
    animation: skeleton-loading 2s linear infinite alternate;
    cursor: wait;
  }
  @keyframes skeleton-loading {
    0% {
      background-color: hsl(200, 20%, 70%);
    }
    100% {
      background-color: hsl(200, 20%, 95%);
    }
  }

  .w-full { width: 100%;  }
  .h-full { height: 100%; }

  .shopify-challenge__container {
    padding: 50px 15px;
  }

  .pagination > * {
    display: none;
  }
</style>
